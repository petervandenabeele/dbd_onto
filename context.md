# Context ontology

First a limited context ontology is constructed. It defines (or reuses) these predicates:

* context:visibility
* context:encryption
* context:license
* dc:source
* dc:creator
* dcterms:created
